pipeline {
    agent {
        label 'docker'
    }
    
    stages {
        stage ('Test') {
            agent {
                docker {
                    // Set both label and image
                    label 'docker'
                    image: 'jenkins/jenkins:lts'
                }
            }
            steps {
                echo 'Run docker-compose file'
                sh 'docker ps -a'
            }
        }
    }
} 
