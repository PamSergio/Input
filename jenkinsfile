pipeline {
    agent any
    
    environment {
    registry = "kammana/my-app:2.0.0"
    }
    
    stages {
        stage ('Build Docker Image') {
            steps {
                docker.build registry + ":$BUILD_NUMBER"
            }
        }
    }
} 
