pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                sh'''
                #!/bin/sh
                node --version
                '''
            }
        }
    }
}
