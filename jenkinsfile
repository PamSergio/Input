node {
    stage('SCM Checkout'){
        git 'https://github.com/PamSergio/Input.git'   
    }
    
    stage ('Prepare environment'){
	        sh("sudo /usr/local/bin/docker ps -a");  
	        //sh("npm install");      
	  }
}
